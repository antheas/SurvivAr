import React, { FunctionComponent } from "react";
import { View } from "react-native";

const CompletedModal: FunctionComponent = () => {
  return <View style={{ height: 200, width: "100%" }} />;
};

export default CompletedModal;
